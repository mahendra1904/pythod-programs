#Enter the String that reverse at own place
s=input('enter the string :   ')
l=len(s)
count=0
for ch in range(l):
    if s[ch].isspace()==False:
        count=count+1
for x in range(count,1,-1):
    print(s[count],end='')
    if s[ch].isspace()==True:
        continue
        
    check    
        
